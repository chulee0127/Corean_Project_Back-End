<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.corean.www.mapper.MainMapper">

	<select id="oneSearch" parameterType="hashmap" resultType="hashmap">
		SELECT
		    1
		FROM TB_COREAN
		<!-- WHERE COREAN_WORD = #{WORD} -->
	</select>
	
</mapper>
